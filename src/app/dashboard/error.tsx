'use client'

/**
 * Dashboard Error Boundary
 *
 * Catches errors specific to the dashboard page and its children
 */

import ErrorBoundary from '@/components/error-boundary'

export default ErrorBoundary
