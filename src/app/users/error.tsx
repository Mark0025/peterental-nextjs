'use client'

/**
 * Users Page Error Boundary
 *
 * Catches errors specific to the users page
 * Particularly useful for calendar connection errors
 */

import ErrorBoundary from '@/components/error-boundary'

export default ErrorBoundary
